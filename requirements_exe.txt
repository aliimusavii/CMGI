# Core requirements for building the executable
# These are the minimum dependencies needed for the GUI application

# GUI and visualization
matplotlib==3.7.2
numpy==1.24.3
pandas==2.0.3

# Machine learning (lightweight versions)
scikit-learn==1.3.0

# For building executable
pyinstaller==5.13.2

# Optional: TensorFlow (can be excluded for smaller executable)
# tensorflow==2.13.0

# Note: TensorFlow is quite large (~500MB). 
# For a smaller executable, you can exclude it and the GUI will show 
# an appropriate message when advanced models are not available.